# 2026-02-25

## DocuMint enterprise loop (cron)

- Hardened team API mutation routes:
  - `src/app/api/teams/create/route.ts`
  - `src/app/api/teams/invite/route.ts`
  - `src/app/api/teams/invite/revoke/route.ts`
- Security hardening in team invites:
  - normalize invite email to lowercase
  - prevent self-invite
  - return sanitized invite payload (no token exposure)
  - improved 409 conflict handling for duplicate invites/members
  - explicit 429 handling for rate limit failures
- Hardened team listing payload:
  - `src/app/api/teams/route.ts` now only includes pending invites for OWNER/ADMIN members.
- Hardened analytics API route:
  - `src/app/api/analytics/docs/route.ts` now has safer payload parsing, sessionId sanitization, and explicit rate-limit/forbidden mapping.
- Added defense-in-depth access control:
  - `src/lib/analytics.ts` now validates team membership before team-scoped analytics query.
  - `src/app/dashboard/analytics/page.tsx` now validates `teamId` view permissions and redirects when unauthorized.
- Wired team analytics UI context:
  - `src/app/dashboard/page.tsx` analytics link now preserves `teamId` query context.
- Polished settings UX:
  - `src/app/dashboard/settings/page.tsx` now includes:
    - Product Updates (marketing emails) toggle
    - Save Notification Preferences button
    - disabled toggle states while save in progress
    - improved notification payload key compatibility on load
    - retry affordance for settings load errors
- Settings API response compatibility:
  - `src/app/api/webhooks/notify/route.ts` GET now returns both legacy and canonical notification keys, plus `marketingEmails`.
- Team UI polish:
  - `src/components/team-management.tsx` improved nullable user typing and removed console.error in action handlers.

## Blockers

- Could not run `git status`, `npm run build`, or create commits because `exec` is unavailable in this runtime:
  - sandbox runtime unavailable
  - gateway/node hosts disallowed
  - no paired node fallback available
- Spawned subagents to attempt build/commit workflow; same execution backend limitation blocked completion.
