# Origin the native shell loads. Defaults to production when unset.
# Point this at a Vercel preview URL or your machine's LAN IP (e.g.
# http://192.168.1.20:3000) to test against a dev build on a real device.
CAP_SERVER_URL="https://www.documintai.dev"
